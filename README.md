# augur_tutorial
Simple tutorial to utilize Augur for basic harmonic-space Fisher forecasts.

Install Augur to a new conda environment via `conda install lsstdesc-augur -c conda-forge` and run the notebook.